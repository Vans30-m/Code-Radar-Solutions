#include <stdio.h>

int main() {
   int N;
   scanf("%d",&N);
   float Area = a*b;
   for (int x=1;x<=N;x++){
    int a,b;
    scanf("%d %d",&a,&b);
    printf("Area = %0.2f\n",Area);
    printf("Perimeter = %0.2f\n",2*(a+b));
   }
    return 0;
}